import React from 'react'


const PostDescription = ({title, body}) => (
    <div>
        <span>
            {title}
        </span>
        <span>
            {body}
        </span>
    </div>
)

export default PostDescription;