import React from 'react'


const PostDescription = ({id}) => (
    <div>
        <span>
            {title}
        </span>
        <span>
            {body}
        </span>
    </div>
)

export default PostDescription;